### 第二步：编写 Python 版本的原生 API 调用

我们将首先编写 Python 版本的调用代码，理解最基础的请求与响应结构。

在 `01_llm_basics/python/` 目录下新建一个文件 **`simple_call.py`**，写入以下代码：

```python
import os
from pathlib import Path
from dotenv import load_dotenv
from openai import OpenAI

# 1. 寻找并加载项目最外层根目录下的 .env 文件
env_path = Path(__file__).resolve().parent.parent.parent.parent / '.env'
load_dotenv(dotenv_path=env_path)

# 2. 初始化 OpenAI 客户端，传入 DeepSeek 的配置
client = OpenAI(
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url=os.getenv("DEEPSEEK_BASE_URL")
)

# 3. 发起对话请求
response = client.chat.completions.create(
    model="deepseek-chat", # DeepSeek 的通用对话模型（即 deepseek-v3）
    messages=[
        {"role": "system", "content": "你是一个简洁的 AI 助手。"},
        {"role": "user", "content": "你好，请用一句话介绍你自己。"}
    ],
    temperature=0.7 # 控制随机性，数值越低回答越严谨
)

# 4. 打印返回的文本内容
print("AI 的回答：")
print(response.choices[0].message.content)
```

---

#### 💡 核心知识点解析：

1. **`messages` 的结构**：大模型的输入是一个消息数组。
   - `system` 角色：设定 AI 的“人设”和行为准则。
   - `user` 角色：用户发送的具体提问。
2. **`response.choices[0].message.content`**：这是大模型接口返回的标准 JSON 嵌套结构。我们通过这个路径提取出纯文本内容。

---

#### 运行与验证：

请确保你的 Python 虚拟环境已激活，然后在 `python` 目录下运行该文件：

```bash
python simple_call.py
```
